# Security policy

Please report suspected token exposure, path escape, workflow permission, or
generated-output vulnerabilities privately through GitHub's security advisory
feature. Do not include active credentials, authorization headers, private
repository metadata, or exploit data in a public issue.

Version 1 receives security fixes on the movable `v1` major tag. Callers that
need immutable supply-chain references should pin a reviewed release commit.
